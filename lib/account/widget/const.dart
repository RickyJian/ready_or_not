// Below defines card enum and const

const total = 'Total';

// Below defines info enum and const

enum accountInfoType { assets, liabilities, netAssets }

const assets = 'Assets';
const liability = 'Liability';
const netAssets = 'Net Assets';
